<h1> New Book </h1>

<% @book.errors.messages.each do |name, message| %>
   <p class="bg-danger"><%name %><%=message.first %></p>
<% end %> 

<%= form_tag '/books' do%>
    <%= render 'form' %>
<%end%>